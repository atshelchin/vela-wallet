// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { DsessReconnectCause } from "./DsessReconnectCause";
import type { DsessRemoteInjectSession } from "./DsessRemoteInjectSession";
import type { DsessTimerKind } from "./DsessTimerKind";

/**
 * What this machine asks the platform to do. The shell holds every live
 * transport object keyed by `session_ref`; the core never sees keys,
 * counters or snapshots. A `session_ref` the shell no longer knows is a
 * no-op (released handles must go quiet — see the module doc's deviations).
 */
export type DsessOperation = { "type": "prepare_wallet_pair", session_ref: number, uri: string, } | { "type": "connect_remote_inject", session_ref: number, session: DsessRemoteInjectSession, } | { "type": "confirm_wallet_pair_join", session_ref: number, } | { "type": "reconnect_transport", session_ref: number, cause: DsessReconnectCause, } | { "type": "disconnect_transport", session_ref: number, } | { "type": "ping_transport", session_ref: number, } | { "type": "push_wallet_info", session_ref: number, chain_id: number, } | { "type": "persist_wallet_pair_counters", session_ref: number, } | { "type": "restore_wallet_pair", session_ref: number, } | { "type": "fetch_dapp_info", session_ref: number, } | { "type": "save_remote_inject_session", session: DsessRemoteInjectSession, } | { "type": "clear_remote_inject_session" } | { "type": "clear_wallet_pair_snapshot" } | { "type": "start_timer", id: number, kind: DsessTimerKind, ms: number, } | { "type": "cancel_timer", id: number, } | { "type": "open_browser", url: string, } | { "type": "alert_invalid_link" };
