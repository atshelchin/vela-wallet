// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { FeedBatchKind } from "./FeedBatchKind";
import type { FeedBatchTransfer } from "./FeedBatchTransfer";
import type { FeedTxStatus } from "./FeedTxStatus";

/**
 * A batch send summarized from its per-line records (`buildBatchView`,
 * `activity.ts:210-236`, ported verbatim including the kind ternary).
 */
export type FeedBatch = { kind: FeedBatchKind, count: number, total_usd: number, transfers: Array<FeedBatchTransfer>, 
/**
 * Stored record ids of the siblings — for live status reconciliation.
 */
ids: Array<string>, from: string, chain_id: number, timestamp: number, status: FeedTxStatus, tx_hash: string, user_op_hash: string, 
/**
 * split only: the single token symbol + its logo.
 */
symbol: string | null, logo_urls: Array<string> | null, 
/**
 * multi_select only: the single recipient.
 */
to: string | null, to_name: string | null, };
