// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { SignFundingNeeded } from "./SignFundingNeeded";
import type { SignQuotedFee } from "./SignQuotedFee";
import type { SignRecord } from "./SignRecord";
import type { SignRecordClose } from "./SignRecordClose";
import type { SignResponsePayload } from "./SignResponsePayload";

/**
 * What this machine asks the platform to do. The shell owns transports (the
 * `transport_id` → instance table), the 15 s pre-check race (a timeout
 * answers `PreCheck { funding: None }`, exactly as the TS race falls through
 * to submit), passkey ceremonies, bundler RPC, storage, and the message-regex
 * classification of submit failures into [`SignSubmitOutcome`].
 *
 * Abortable operations (`CheckBundlerFunding`, `AttemptSponsorship`,
 * `SignAndSubmit` pre-passkey) must honour the bridge's
 * `cancelled_effect_ids` channel for a true abort.
 */
export type SignOperation = { "type": "send_response", transport_id: string, id: string, payload: SignResponsePayload, } | { "type": "check_bundler_funding", chain_id: number, account: string, bundler_cost_wei: string | null, bust_cache: boolean, } | { "type": "attempt_sponsorship", funding: SignFundingNeeded, force: boolean, } | { "type": "sign_and_submit", id: string, method: string, 
/**
 * FINAL (capped) params — invariant ⑨.
 */
params_json: string, chain_id: number, address: string, credential_id: string, max_fee_per_gas: string | null, gas_fee_token: string | null, quoted_fee: SignQuotedFee | null, } | { "type": "persist_record", record: SignRecord, } | { "type": "update_record", record_id: string, close: SignRecordClose, } | { "type": "switch_active_account", index: number, };
