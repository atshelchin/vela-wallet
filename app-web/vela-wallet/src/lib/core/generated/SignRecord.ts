// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { SignRecordKind } from "./SignRecordKind";
import type { SignRecordStatus } from "./SignRecordStatus";

/**
 * The durable history record. The shell maps this onto `LocalTransaction`
 * (`buildSigningRecord`) and owns `capRequest` clipping and asset-sim
 * serialisation; `params_json` is always the FINAL (capped) params
 * (invariant ⑨).
 */
export type SignRecord = { 
/**
 * `dapp-<ms>-tx|typed|msg`, the TS id scheme verbatim.
 */
record_id: string, kind: SignRecordKind, method: string, params_json: string, 
/**
 * Result of the request — `""` while pending.
 */
result: string, from: string, chain_id: number, now_ms: number, status: SignRecordStatus, user_op_hash: string, dapp_origin: string, intent: string | null, };
