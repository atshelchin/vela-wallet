// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { TrustCustomToken } from "./TrustCustomToken";
import type { TrustLogsOutcome } from "./TrustLogsOutcome";
import type { TrustMetaEntry } from "./TrustMetaEntry";

/**
 * What the shell observed. Scan results carry the address they were fetched
 * for; a result tagged with another account is dropped by construction.
 */
export type TrustShellResult = { "type": "block_number", address: string, chain_id: number, 
/**
 * `eth_blockNumber` hex, or `None` on RPC failure.
 */
block_hex: string | null, } | { "type": "logs", address: string, chain_id: number, outcome: TrustLogsOutcome, } | { "type": "block_timestamp", address: string, chain_id: number, block_number: number, 
/**
 * Unix seconds from the block header, or `None` when the lookup
 * failed — the transfer then falls back to "now".
 */
timestamp_sec: number | null, 
/**
 * Epoch ms, carried by the result (the 011 `now_iso` pattern) so the
 * core never reads a clock.
 */
now_ms: number, } | { "type": "erc_meta", chain_id: number, entries: Array<TrustMetaEntry>, } | { "type": "custom_tokens", tokens: Array<TrustCustomToken> | null, } | { "type": "token_written", ok: boolean, } | { "type": "cache_invalidated" };
