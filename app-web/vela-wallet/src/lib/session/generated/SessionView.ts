// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { SessionAccountRow } from "./SessionAccountRow";
import type { SessionRoute } from "./SessionRoute";
import type { SessionSignOutView } from "./SessionSignOutView";

export type SessionView = { 
/**
 * `isLoading` — true until storage has been read.
 */
loading: boolean, has_wallet: boolean, 
/**
 * The active account's address, `""` when there is none — derived, so it
 * is `accounts[active_index].address` by construction (invariant ①).
 */
address: string, active_index: number, accounts: Array<SessionAccountRow>, allowed_route: SessionRoute, 
/**
 * `Some` ⇒ the sign-out confirmation dialog is open.
 */
sign_out: SessionSignOutView | null, };
