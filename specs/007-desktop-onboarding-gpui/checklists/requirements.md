# Checklist: Requirements & Quality Gates

## CHK-SCOPE (FR-011)
- [x] Diff touches only: `app-desktop/vela-wallet/**`, `specs/007…/**`,
  `design/onboarding/**`, `rust/crates/vela-core/i18n/locales/**`, and files
  regenerated by `gen-i18n.mjs` (`rust/.../src/i18n/paths.rs`,
  `rust/.../src/i18n_catalogs/*.rs`, `src/i18n/resources.ts`,
  `public/i18n/*.json`), plus the generator's own tripwire constants in
  `scripts/gen-i18n.mjs` (corpus size is asserted, so growing the corpus
  means updating the assertion).
- [x] No hand edits inside generated files (headers say GENERATED).

## CHK-TOKENS (FR-002)
- [x] `rg -n '0x[0-9a-fA-F]{6}' app-desktop/vela-wallet/src --glob '!theme.rs' --glob '!logo.rs'`
  returns nothing (logo geometry colors are theme-passed; only theme.rs names hex).
- [x] No `px(...)` literal in `onboarding.rs` that isn't a named constant from
  `theme.rs` (grid geometry constants included).

## CHK-I18N (FR-005/006/007)
- [x] `rg -n '"[^"]*[一-鿿]' app-desktop/vela-wallet/src` → the only hits are
  the two `#[cfg(test)]` assertions in `loc.rs` pinning the mock's zh copy
  against the catalog (they are checks, not UI strings). Render code has no
  CJK/English UI literal; permitted literals: key paths, "Vela Wallet", env
  names, numeral format string.
- [x] All 15 locale files gained exactly the same 13 keys (gen-i18n asserts).
- [x] `VELA_LANG=zh|en|de` renders fully translated screens; no `onboarding.`
  echo visible.

## CHK-CONTRAST (SC-005) — computed pairs, both themes
- [x] fg_base on bg_base ≥ 4.5
- [x] fg_muted on bg_base ≥ 4.5
- [x] fg_muted on bg_raised ≥ 4.5
- [x] fg_subtle on bg_raised ≥ 3.0 (12 px numeral is decorative-adjacent; target 3+)
- [x] fg_inverse on accent ≥ 3.0 (brand-accent pairing, DV-004)
- [x] secondary label on bg_raised ≥ 4.5 (DV-001 is what makes dark pass)

## CHK-STATES (FR-004)
- [x] Primary: base/hover/active distinct in both themes; label stays fg_inverse.
- [x] Secondary: border constant, fill base→sunken on hover; label fg_base.
- [x] Both buttons: `cursor_pointer()`.

## CHK-VISUAL (SC-002) — reference points vs mocks
- [x] Column divider at x ≈ 765/1280; panel width 512.
- [x] Card grid 3×2 at 204×140 (design size; cards flex wider with the window), gaps 14/16, origin (96, 300).
- [x] Buttons 345×52 capsules at panel inset 84.
- [x] Palette spot-check: bg_base, bg_raised, accent, card body text — both modes.
